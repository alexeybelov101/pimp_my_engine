# pimp_my_engine
Симулятор газодинамических процессов в двигателе внутреннего сгорания.

## Статус проекта
Проект находится в активной разработке. Интерфейсы и структура могут изменяться без предварительного уведомления.

## Требования
Для сборки требуются:
- компилятор GCC с поддержкой стандарта C++26;
- библиотека nlohmann/json (заголовочная, устанавливается отдельно).

## Сборка
Проект использует Makefile. Доступны следующие цели:
- make или make release — сборка оптимизированной версии
- make debug — сборка отладочной версии
- make run — сборка и запуск
- make debug run - сборка и запуск отладочной версии
- make clean — удаление артефактов сборки

Пример:
```bash
make release
./bin/release/pimp_my_engine --config configs/config.json
```

## Использование
Запуск симуляции с параметрами по умолчанию:
```bash
./bin/release/pimp_my_engine
```

### Доступные опции командной строки:
    -t, --time TIME — время симуляции в секундах.
    -f, --freq FREQ — частота симуляции в герцах.
    -r, --rpm RPM — начальные обороты маховика.
    -c, --config PATH — путь к файлу конфигурации.
    -h, --help — справка.

## Конфигурация
Параметры двигателя и впускной/выпускной систем задаются в JSON-файле (по умолчанию configs/config.json).
Размеры деталей взяты из открытых источников для двигателя 21011 и могут не соответствовать реальным.
Все размеры указываются в миллиметрах, граммах и градусах.

## Лицензия
Проект распространяется под лицензией AGPL-3.0-or-later. Полный текст лицензии: [COPYING](COPYING)

## TODO:
- Инкапсуляция логики обновления угла поворота коленвала
- Рефакторинг проекта для моделирования различных схем двигателей (рядная, V-образная, W-образная, VR-образная, оппозитная, звездообразная)
- Расчёт потоков для соединений из 3 и более труб
- Дроссельная заслонка
- Система зажигания
- Топливная система
- Расчёт масс воздуха, топлива и отработавших газов
- Процесс горения
- Система изменения фаз газораспределения
- Учёт инерции коленвала, шатунов и поршней
- Учёт трения
- Учёт тепловыделения и теплообмена
- Система охлаждения
- Параллелизация вычислений
- Создание API для использования проекта в качестве библиотеки
