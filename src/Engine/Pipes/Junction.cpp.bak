#include "Junction.hpp"

Junction::Junction(const std::vector<IPhisycsNode>& nodes):
    nodes_(nodes) {}

void Junction::step() {
    std::vector<IBoundaries> boundaries;
    boundaries.reserve(nodes_.size());

    for (auto& node : nodes_) {
        boundaries.push_back(node.getBoundary());
    }
    ...
}
