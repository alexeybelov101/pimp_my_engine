# Engine Configuration for VAZ-21011
# Like Quake .cfg files

# === Geometry ===
engine.bore_mm = 79.0
engine.stroke_mm = 66.0
engine.rod_length_mm = 126.0
engine.chamber_volume_cc = 43.1
engine.cylinder_count = 4

# === Friction and Losses ===
engine.friction_coefficient = 0.10

# === Air-Fuel Ratio ===
engine.afr_stoich = 14.7

# === Pressure Parameters ===
engine.base_pressure_bar = 48.0
engine.k_polytropic = 1.27

# === Scaling ===
engine.torque_scale = 1.0
engine.power_scale = 1.0

# === VE Curve (rpm:ve pairs) ===
# Format: rpm, ve, rpm, ve, ...
engine.ve_curve = 600, 0.50, 3500, 0.88, 5500, 0.72, 7000, 0.45

# === Firing Order (crank angles in degrees) ===
engine.firing_order = 0.0, 180.0, 360.0, 540.0
